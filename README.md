# Document_AI
An AI-powered invoice parsing system using Donut (Document Understanding Transformer) and Google Gemini API to automatically extract key fields such as vendor name, buyer name, invoice number, dates, totals, and more from PDF or image invoices. Includes preprocessing, model inference, and structured JSON output generation.
